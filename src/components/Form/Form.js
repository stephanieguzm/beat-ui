import './Form.css'

// get user section selection
// pass section up to App
// useEffect() to re-render results
// then passed down to Stories

export const Form = ({ setSectionSelection, sections }) => {
  
  // const sectionOptions = 

  return (
    <form className='form-container'>
      
    </form>
  )
}
